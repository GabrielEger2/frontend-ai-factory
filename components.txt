SiteGen Component Library — Missing Components
================================================
Snapshot date: 2026-05-06 (refreshed after CTA / testimonial / contact /
carousel / motion / stats / faq / footer batch.)
Current state: 71 / ~75 (target from .claude/CLAUDE.md)
Gap: 0 across the canonical 12 categories — every category sits at or
above its target (only `layout/grid` is +2 over, see follow-up #2).
All 71 metadata.json files pass `npm run validate:metadata`.
TypeScript `tsc --noEmit` passes clean across all new components.
`npm run build:manifest` writes 71 entries.

Coverage vs target
------------------
Category        Target  OnDisk  Gap
hero            15      15      done
layout/split    8       8       done
cta             8       8       done
testimonial     6       6       done
contact         5       5       done
carousel        5       5       done
motion          4       4       done
stats           4       4       done
faq             4       4       done
footer          3       3       done
navigation      4       4       done
layout/grid     4       6       +2 over

Components added in this batch (16)
-----------------------------------
CTA (3)
  - cta/CtaCountdown — `cta-countdown-01` — live countdown to a deadline
  - cta/CtaDualOfferSplit — `cta-dual-offer-split-01` — two side-by-side offer cards
  - cta/CtaStickyBanner — `cta-sticky-banner-01` — fixed-bottom CTA, scroll-triggered

Testimonial (3)
  - testimonials/TestimonialVideoCard — `testimonial-video-card-01` — 1-3 video tiles + lightbox iframe
  - testimonials/TestimonialSpotlightQuote — `testimonial-spotlight-quote-01` — hero pull-quote
  - testimonials/TestimonialLogoQuoteRibbon — `testimonial-logo-quote-ribbon-01` — logo strip + cross-fading quote

Contact (3)
  - contact/ContactSplitForm — `contact-split-form-01` — form + structured info column
  - contact/ContactBookingEmbed — `contact-booking-embed-01` — Cal.com/Calendly iframe
  - contact/ContactSupportTabs — `contact-support-tabs-01` — sales/support/press routing tabs

Carousel (2)
  - carousel/CarouselBeforeAfter — `carousel-before-after-01` — drag-reveal comparison slider
  - carousel/CarouselTestimonialAvatarPeek — `carousel-testimonial-avatar-peek-01` — hero quote + avatar strip

Motion (2)
  - motion/MotionMarquee — `motion-marquee-01` — words / logos / badges scrolling ticker
  - motion/MotionScrollNarrative — `motion-scroll-narrative-01` — pinned-image narrative

Stats (1)
  - stats/StatsChart — `stats-chart-01` — animated bar chart or sparkline trend

FAQ (1)
  - faq/FaqTabbed — `faq-tabbed-01` — categorized FAQ with animated underline tab strip

Footer (1)
  - footers/FooterMega — `footer-mega-01` — newsletter + 4-col sitemap + legal/social bar

Structural follow-ups (unchanged)
---------------------------------
1. `variants/` directories: CLAUDE.md describes them as optional ("same
   component, different density/color mode"), but 0 of 71 components have one.
   Decision needed: drop the convention from CLAUDE.md, or seed dense/dark
   variants for 2-3 high-value components (e.g. HeroSplitImage,
   CtaEditorialSplit, FaqAccordion).

2. layout/grid is over target (6 vs 4). PricingTiers, ProcessTimeline,
   LogoCloud, IconFeatureGrid arguably belong in their own categories
   (`pricing`, `process`, `partners`, `features`). Decision needed: raise the
   layout/grid target in CLAUDE.md, or split these into dedicated categories
   matching how their `purpose[]` already tags them.

3. The total of 71 is *under* the ~75 target only because hero already
   ships 15 (= target) and layout/grid is +2 over. Future additions
   should focus on the structural follow-ups above rather than padding
   counts.

SiteGen Component Library — Missing Components
================================================
Snapshot date: 2026-05-06
Current state: 74 / ~112 (target from .claude/rules/domains/components.md)
Gap: 38 components across 15 categories (3 brand-new categories)
Note: ParallaxContent and StickyCards were extracted to components/wrappers/
as hand-authored page-template wrappers (not Composer-pickable).

Coverage vs target
------------------
Category         Target  OnDisk  Gap
pricing (new)    5       0       -5
gallery (new)    5       0       -5
team (new)       4       0       -4
cta              12      8       -4
hero             18      15      -3
testimonial      8       6       -2
footer           5       3       -2
faq              6       4       -2
contact          7       5       -2
navigation       6       4       -2
carousel         7       5       -2
motion           6       2       -4
layout/grid      8       6       -2
layout/split     10      8       -2
stats            5       4       -1

Missing components (suggested)
------------------------------

Pricing (-5) — NEW CATEGORY, build from scratch
  - PricingTierCards — 3-column tier cards with feature lists, "most popular" highlight
  - PricingComparisonTable — full feature-row × tier-column matrix
  - PricingFreemiumLadder — visual ladder/staircase showing tier progression
  - PricingSinglePrice — solo card for one-product / agency packages
  - PricingMonthlyAnnualToggle — animated billing-cycle switch with discount badge
  Note: when this category lands, migrate `layout/grid/PricingTiers` into it.

Gallery (-5) — NEW CATEGORY, build from scratch
  - GalleryMasonry — Pinterest-style asymmetric grid
  - GalleryLightboxGrid — uniform grid with click-to-expand modal
  - GalleryCaseStudy — long-form case-study layout (problem / solution / result)
  - GalleryPortfolioStrip — horizontal-scroll portfolio with hover meta
  - GalleryImageTextEditorial — alternating image + caption editorial spread

Team (-4) — NEW CATEGORY, build from scratch
  - TeamLeadershipGrid — portrait grid with role / name / short title
  - TeamFounderSplit — single-founder split with extended bio + signature
  - TeamCarousel — horizontally-scrolling team cards with peek
  - TeamMemberSpotlight — featured member with quote + credentials

CTA (-4)
  - CtaTestimonialPaired — CTA with embedded social-proof quote
  - CtaVideoBackdrop — looping video behind primary CTA
  - CtaInlineCalculator — interactive estimate widget (mortgage / savings / quote)
  - CtaFormInline — form-as-CTA with inline lead-capture fields

Hero (-3)
  - HeroDeviceChrome — product screenshot in browser / device frame
  - HeroKineticType — large animated typography with morphing words
  - HeroIllustration — spot-art / illustration-driven hero for playful brands

Testimonial (-2)
  - TestimonialMasonryQuotes — Pinterest-style mass social-proof wall
  - TestimonialMetricCard — testimonial paired with quantified KPI outcome

Footer (-2)
  - FooterMinimalStrip — single-line copyright + social icons
  - FooterColumnsSocial — multi-column utility footer with social handles

FAQ (-2)
  - FaqSearch — search-driven with live filter and keyword highlight
  - FaqCategorized — sidebar category navigation + grouped questions

Contact (-2)
  - ContactChatEmbed — embedded chat / WhatsApp / Intercom widget
  - ContactMultiStep — wizard-style routing form (sales / support / partnership)

Navigation (-2)
  - NavbarTransparentOverlay — transparent over hero, solidifies on scroll
  - NavbarSideRail — vertical fixed left/right rail (portfolio / docs sites)

Carousel (-2)
  - CarouselThumbnailNav — main image with thumbnail strip (e-com)
  - CarouselFullBleedSlider — edge-to-edge image rotator with caption overlay

Motion (-4)
  - MotionRevealOnScroll — staggered text/element reveal on scroll
  - MotionPinnedSection — pinned canvas with scroll-driven content swap
  - (replacement for ParallaxContent) — single-section parallax story with data slots
  - (replacement for StickyCards) — single sticky-card panel with data slots

Layout/grid (-2)
  - GridBentoLayout — bento-box asymmetric feature grid
  - GridFeatureCompare — comparison grid (rows = features, cols = options)

Layout/split (-2)
  - ComparisonSplit — IMPLEMENT the existing empty folder at
    `components/library/layouts/split/ComparisonSplit/` (us vs them, side-by-side)
  - SplitQuoteImage — large editorial quote with portrait / imagery split

Stats (-1)
  - StatsTimelineMilestones — year-by-year achievement timeline with stats

Structural follow-ups
---------------------
1. New category enablement (paired commit required before any component
   can declare these categories):
   - Add `pricing | team | gallery` to validator category enum.
   - Add to FLOW_ORDER in `agents/scripts/generate-pair-scores.ts`.
   - Add to dashboard category facets.

2. New purpose tokens needed (paired commit per rules-doc convention):
   - `pricing`     — pricing-tier / comparison-table / freemium slots
   - `gallery`     — masonry / lightbox / case-study slots
   - `comparison`  — side-by-side feature comparisons (also unblocks ComparisonSplit)
   Update: rules doc canonical list, `CANONICAL_PURPOSE` in
   `components/scripts/validate-metadata.ts`, and the matching constant in
   `components/tests/metadata-schema.test.ts` — all in the same commit.

3. Manifest ID hygiene — drift from naming convention, fix when convenient:
   - Duplicated category prefix:
       hero-hero-bold-editorial-01, cta-collage-duo-01,
       cta-editorial-split-01, contact-locations-map-01,
       contact-shapes-form-01
   - Wrong prefix on testimonials (using `layout-`):
       testimonial-infinitescroll-01, testimonial-stackedsplit-01, testimonial-staggerfan-01
       → should be `testimonial-...`

4. Variants — only 4 heroes currently have `variants/` (geometric,
   parallax-images, shuffle-cards, split-image). Either drop the
   variants convention from the rules doc, or seed compact/airy variants
   for top-picked components (HeroBoldEditorial, HeroVideoBackdrop,
   CtaEditorialSplit, CtaImageBackdrop, FooterMega).

5. layout/grid migration — when `pricing` ships, move PricingTiers
   from `layout/grid` to `pricing`. If LogoCloud / award-style trust
   components grow past 3, consider promoting them out of `layout/grid`
   into a dedicated `trust` category (currently NOT in the targets).

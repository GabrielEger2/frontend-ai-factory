{
  "id": "pricing-monthly-annual-toggle-01",
  "name": "Pricing Monthly Annual Toggle",
  "category": "pricing",
  "purpose": ["pricing", "products", "services"],
  "acceptsStyleKit": {
    "card": false,
    "background": false,
    "textDecoration": false,
    "button": true
  },
  "style": ["modern", "corporate", "playful", "minimal"],
  "mood": ["professional", "energetic", "trustworthy"],
  "layout": "grid",
  "density": "medium",
  "imageWeight": 0,
  "slots": [
    { "name": "label", "type": "text", "maxLength": 40, "optional": true },
    { "name": "headline", "type": "text", "maxLength": 80 },
    {
      "name": "description",
      "type": "text",
      "maxLength": 220,
      "optional": true
    },
    {
      "name": "monthlyLabel",
      "type": "text",
      "maxLength": 20,
      "optional": true
    },
    {
      "name": "annualLabel",
      "type": "text",
      "maxLength": 20,
      "optional": true
    },
    {
      "name": "annualDiscountBadge",
      "type": "text",
      "maxLength": 24,
      "optional": true
    },
    {
      "name": "defaultCycle",
      "type": "text",
      "maxLength": 10,
      "optional": true
    },
    {
      "name": "plans",
      "type": "list",
      "maxItems": 4,
      "itemSchema": {
        "type": "object",
        "fields": [
          { "name": "name", "type": "text", "maxLength": 30 },
          {
            "name": "description",
            "type": "text",
            "maxLength": 140,
            "optional": true
          },
          { "name": "monthlyPrice", "type": "text", "maxLength": 20 },
          { "name": "annualPrice", "type": "text", "maxLength": 20 },
          {
            "name": "annualTotal",
            "type": "text",
            "maxLength": 40,
            "optional": true
          },
          {
            "name": "badge",
            "type": "text",
            "maxLength": 24,
            "optional": true
          },
          { "name": "ctaText", "type": "text", "maxLength": 30 },
          { "name": "ctaUrl", "type": "url" },
          {
            "name": "features",
            "type": "list",
            "maxItems": 8,
            "itemSchema": { "type": "text", "maxLength": 80 }
          },
          { "name": "featured", "type": "boolean", "optional": true }
        ]
      }
    },
    { "name": "footnote", "type": "text", "maxLength": 200, "optional": true }
  ],
  "mobileBehavior": "stack",
  "pairsWell": [
    "faq-accordion-01",
    "faq-tabbed-01",
    "cta-minimal-strip-01",
    "testimonial-logo-quote-ribbon-01"
  ],
  "pairsPoorly": [
    "pricing-tier-cards-01",
    "pricing-comparison-table-01",
    "pricing-freemium-ladder-01",
    "layout-pricing-tiers-01"
  ],
  "description": "Tier cards with an animated monthly / annual billing-cycle switch. The toggle uses a shared-layoutId sliding pill; per-plan prices cross-fade on cycle change; an annual discount badge inverts when active. Designed to make the savings legible without forcing the buyer to read math.",
  "descriptions": {
    "descriptive": "Three-up plan cards with a centered pill-toggle above the grid. The toggle uses Framer Motion's layoutId so the active selection slides between Monthly and Annual with spring physics. Each plan card cross-fades the price on cycle change; an optional 'billed yearly' total fades in beneath the per-month figure when annual is selected. Featured plan lifts -3 to break the horizon line.",
    "usage": "Use on subscription products where the annual upsell is real (15-25% off) and worth showcasing. Place mid-page after the features section. Pairs poorly with other pricing components — pick one pricing block per page so the toggle is unambiguous.",
    "audienceFit": "B2B SaaS, creator subscriptions, fintech subscriptions, productivity tools. Suits buyers who can be nudged to annual but want to confirm monthly first."
  }
}

{
  "name": "@sitegen/infra",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "synth": "cdk synth",
    "deploy": "cdk deploy --all"
  },
  "dependencies": {
    "aws-cdk-lib": "^2",
    "constructs": "^10",
    "esbuild": "^0.24"
  },
  "devDependencies": {
    "@types/node": "^20",
    "aws-cdk": "^2",
    "ts-node": "^10",
    "typescript": "^5"
  }
}
